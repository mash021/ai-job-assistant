# Backend dependencies for AI Job Assistant.
# Pinned to known-good versions for reproducible Docker builds.

fastapi==0.115.6            # Web framework that powers the API
uvicorn[standard]==0.34.0   # ASGI server used to run FastAPI

# ----- Configuration -----
pydantic-settings==2.7.1    # Typed settings loaded from environment variables

# ----- Database -----
SQLAlchemy==2.0.36          # ORM and database toolkit
alembic==1.14.0             # Database schema migrations
psycopg2-binary==2.9.10     # PostgreSQL driver
